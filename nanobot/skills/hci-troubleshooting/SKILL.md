---
name: hci-troubleshooting
description: HCI 通用排障工作流与输出约束，强调只读优先、证据优先、风险说明。
metadata: {"nanobot":{"always":true}}
---

# HCI Troubleshooting

适用于 HCI 场景下的通用排障对话。

## 工作流

1. 先复述现象和影响范围。
2. 先确认排查目标是本机还是远程主机。
3. 再收集只读证据。
3. 区分已证实事实与推断。
4. 最后给出下一步建议和风险说明。

## 输出结构

回答优先包含：

- 现象
- 已证实证据
- 推断
- 建议动作
- 风险说明

## 工具使用建议

- 优先使用 `diagnose_log_read`、`diagnose_log_search`、`diagnose_system_status`
- 需要历史经验时再使用 `search_cases` / `get_case`
- 需要结构化行动方案时使用 `plan`

## 远程只读排查规则

当排查目标不是当前运行 HCIGuard 的机器时，按以下规则执行：

1. 先明确目标主机、角色、当前现象是否都来自同一台机器。
2. 优先使用平台已允许的远程只读能力，不要把远程执行描述成已经正式支持的任意 SSH 平台能力。
3. 若现场通过免密 SSH 接入远程主机，仍然只允许读取日志、查看服务状态、查看系统状态，不直接做变更。
4. 在回答中必须标明证据来自哪台主机，避免把本机证据和远程证据混淆。
5. 远程场景下先做事实采集，再给结论；不要因为已有历史案例就跳过采证。

远程场景优先检查：

- 目标主机上的关键日志
- 目标主机上的相关服务状态
- 目标主机上的系统状态
- 与当前问题时间窗口对应的错误片段

远程场景禁止事项：

- 不在对话、案例、历史记录中记录明文密码
- 不把“已能 SSH 上去”等同于“已确认根因”
- 不在证据不足时建议直接重启、停止或修改远程服务

## 禁止事项

- 不要在证据不足时直接下结论
- 不要把历史案例当作当前事实
- 不要绕过只读与审批规则
- 不要混淆本机与远程主机证据来源
